{
  "name": "code-space",
  "version": "0.1.0",
  "private": true,
  "description": "Local Code Space wrapper around code-server",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test"
  },
  "engines": {
    "node": ">=18"
  }
}
